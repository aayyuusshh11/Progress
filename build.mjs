import {cp,rm,mkdir} from 'node:fs/promises';const d=new URL('./dist/',import.meta.url);await rm(d,{recursive:true,force:true});await mkdir(d,{recursive:true});for(const f of ['index.html','manifest.json','sw.js'])await cp(new URL(`./${f}`,import.meta.url),new URL(`./dist/${f}`,import.meta.url));await cp(new URL('./public/',import.meta.url),new URL('./dist/',import.meta.url),{recursive:true});await cp(new URL('./src/',import.meta.url),new URL('./dist/src/',import.meta.url),{recursive:true});console.log('build complete');
