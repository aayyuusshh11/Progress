import http from 'node:http';import fs from 'node:fs/promises';import path from 'node:path';
const root=process.argv.includes('--dist')?path.join(process.cwd(),'dist'):process.cwd(),port=Number(process.env.PORT||5173);
const types={'.html':'text/html; charset=utf-8','.js':'text/javascript; charset=utf-8','.css':'text/css; charset=utf-8','.json':'application/json; charset=utf-8','.svg':'image/svg+xml','.png':'image/png','.webmanifest':'application/manifest+json'};
http.createServer(async(req,res)=>{try{const u=new URL(req.url||'/',`http://${req.headers.host||'localhost'}`);let p=decodeURIComponent(u.pathname);if(p==='/')p='/index.html';const rootAbs=path.resolve(root),file=path.resolve(rootAbs,'.'+p);if(!file.startsWith(rootAbs+path.sep)&&file!==rootAbs){res.writeHead(403);return res.end('Forbidden')}let target=file;try{const st=await fs.stat(target);if(st.isDirectory())target=path.join(target,'index.html')}catch{const pubFile=path.resolve(rootAbs,'public','.'+p);if(pubFile.startsWith(path.resolve(rootAbs,'public')+path.sep)){try{await fs.stat(pubFile);target=pubFile}catch{res.writeHead(404,{'Content-Type':'text/plain; charset=utf-8'});return res.end('Not found')}}else{res.writeHead(404,{'Content-Type':'text/plain; charset=utf-8'});return res.end('Not found')}}res.writeHead(200,{'Content-Type':types[path.extname(target)]||'application/octet-stream','Cache-Control':'no-store'});res.end(await fs.readFile(target))}catch{res.writeHead(500);res.end('Server error')}}).listen(port,'0.0.0.0',()=>console.log(`PROGRESS: http://localhost:${port}`));
